#include <stdio.h>

int main() {
    char text[] = "hello";
    printf ("%s\n", text);

    char *buf;
    char *after;
    
    char test[]="cvdaba";

    long long s;
       
    //simple_strtoul(buf, &after, 10); 
    return 0;
}
